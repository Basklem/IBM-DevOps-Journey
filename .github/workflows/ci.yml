name: CI

on:
  push:
    paths:
      - '01-intro-to-devops/project/**'
  pull_request:
    paths:
      - '01-intro-to-devops/project/**'

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        working-directory: 01-intro-to-devops/project/app
        run: pip install -r requirements.txt

      - name: Lint with ruff
        working-directory: 01-intro-to-devops/project/app
        run: ruff check .

      - name: Run tests
        working-directory: 01-intro-to-devops/project/app
        run: pytest -v
